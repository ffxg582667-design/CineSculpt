flask>=3.0
flask-cors>=5.0
numpy>=1.26
Pillow>=10.0
gunicorn>=22.0

# 网格处理核心
trimesh>=4.0
# trimesh 可选依赖（必需）：简化 / 补洞 / 连通体拆分
fast-simplification>=0.1
scipy>=1.11
networkx>=3.0

# 3D 重建（锁定版本，保证与本地一致）
tripo3d==0.4.2

# rembg(u2net 抠图, 约 180MB) 已在云端移除：免费档 512MB 内存会被 OOM 杀进程。
# segment.py 已做优雅降级——import 失败时自动退回原图重建，其余功能不受影响。
