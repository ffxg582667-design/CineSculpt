flask>=3.0
flask-cors>=5.0
numpy>=1.26
Pillow>=10.0
trimesh>=4.0
tripo3d>=1.0
gunicorn>=22.0

# rembg 用于「抠主体」(u2net 模型, 约 180MB, 内存占用较大)。
# 若 Render 免费档(512MB) 部署报 Out of memory，删掉下面这行重新部署即可：
# 应用会自动降级为「不抠图，直接用原图重建」，其余功能不受影响。
rembg>=2.0
