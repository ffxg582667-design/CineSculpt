# CineSculpt 开发过程记录

> 电影场景 3D 打印 AI Agent —— MakerMuse 黑客松项目
> 定位：上传电影场景截图，AI 重建 3D 模型，微调后导出 STL 打印成实体摆件
> 核心价值：把电影带来的情绪，变成可触摸的物理纪念品

---

## 一、项目起源与方向转变

项目最初是一个「激光雕刻 AI 一条龙工具」（图片转 SVG 矢量文件），完成了图片上传、抠图、Potrace 转 SVG 的网页 Demo。

后来方向转变为 **CineSculpt** —— 面向 3D 打印爱好者的电影场景重建工具，从「2D 矢量」转为「2D 图片生成 3D 模型」。

### 关键定位澄清
早期就确立了诚实的产品定位：本产品是「生成 / 再创作」，而非「毫米级精确还原」。AI 3D 重建本质是根据图片合理「脑补」出 3D 模型，用户追求的是情绪共鸣与审美，不是精确复刻。

---

## 二、技术架构

| 层 | 技术 | 说明 |
| --- | --- | --- |
| 前端 | HTML + Three.js（ES module + importmap） | 3D 预览、旋转、缩放 |
| 后端 | Python + Flask（threaded=True） | 提供 REST API |
| 3D 重建 | Tripo3D API（官方 SDK） | 图生 3D / 文生 3D |
| 文字理解 | OpenAI 兼容 LLM（默认 DeepSeek） | 把「出处」扩写成 AI 理解 |
| 抠图 | rembg（alpha matting + 白底合成） | 主角模式去背景 |
| 网格处理 | Trimesh + fast_simplification | 修复、简化、导出 STL |
| 公网访问 | Cloudflare Tunnel | 生成可分享的公网地址 |

### 文件结构
- backend/app.py —— 主服务，所有 API 路由
- backend/reconstruct.py —— 3D 重建（图生/文生 + 占位 fallback）
- backend/describe.py —— LLM 提示词生成（OpenAI 兼容）
- backend/segment.py —— rembg 抠图
- backend/mesh_ops.py —— 网格操作（修复/加底座/缩放/简化/打印性检测/导出）
- frontend/index.html / style.css / app.js —— 前端页面与交互
- start.bat / stop.bat —— 一键启动 / 停止

---

## 三、开发历程（按时间顺序的关键里程碑）

### 1. 搭建完整链路
搭通了「上传图片 → 3D 重建 → Three.js 预览 → 微调 → 导出 STL」的完整流程。3D 重建采用可插拔接口设计：默认返回占位模型（保证演示不中断），可切换真实 API。

### 2. 反复出现的中文乱码（问号）问题
最棘手的工程问题之一。用 PowerShell 管道向 Python 写文件时，中文经 stdin 传输被替换成问号。
- **根因**：控制台输入编码不是 UTF-8。
- **最终解法**：所有中文改用 `\u` Unicode 转义写入源码（源码全 ASCII，PowerShell 无法破坏），前后端所有文件的中文全部修复。

### 3. 字体与视觉
- 先统一为 Space Grotesk（科技感）。
- 后按需求改为 **Cinzel + Noto Serif SC**（哥特电影史诗风 + 衬线设计感），表单元素保持无衬线体保证可读性。
- 标题 CineSculpt 字间距收紧。

### 4. 依次添加图片
上传区支持一张一张陆续添加（最多 5 张）、每张可单独删除，不再是一次性选完。

### 5. 接入真实 3D 重建（踩坑记录）
- 先试 **Meshy**：免费额度只能网页用，API 是独立付费池，官方测试 key 已失效 —— 放弃。
- 改用 **Tripo3D**：免费额度可用于 API，官方 Python SDK 封装了上传/轮询/下载。实测单图重建成功（`source=real`，生成 12MB 真实 PBR 模型）。
- 关键发现：免费积分分「Free Wallet」和「API Wallet」两个池，`get_balance` 只查 API Wallet，导致一度误判「余额为 0」。

### 6. 修复「微调 404」
重建/微调/导出原本依赖内存字典存 session，服务一重启内存清空就丢。
- **解法**：改为基于磁盘文件（模型存在 output/{id}.glb），服务重启也不受影响；前端锁定「已重建的 session id」，不受后续增删图片影响。

### 7. 修复多图重建失败
上传多张图走 Tripo 的 multiview_to_model，参数格式严格导致 HTTP 400。
- **解法**：统一改用单图重建（image_to_model），稳定可靠。

### 8. 微调改造为「AI 修理 + 人工微调」两步
- 新增独立「AI 一键修理」按钮：去碎片、补洞、修法线、水密处理，并返回修复报告。
- 「人工微调」：加底座、目标高度缩放、网格简化。

### 9. AI 修理卡死问题（重大性能问题）
体素重网格化用 128 分辨率（约 200 万体素），在内存偏紧的机器上把系统拖垮，`import trimesh` 都要 60 秒以上，需重启恢复。
- **优化（方案 D）**：预简化到 5000 面 + 体素分辨率降到 48 + 尺寸归一化 + Flask 多线程 + 补装 fast_simplification。
- **效果**：从「卡死不返回」降到约 12 秒完成，模型水密可打印。
- 后因内存反复被榨干，进一步改为**轻量补洞方案**（去掉吃内存的体素重建），并在重建后自动归一化到 80mm。

### 10. 接入路线 B：文字辅助生成
用户提供 DeepSeek API，用 LLM 把「出处」（如「星际穿越 永恒号飞船」）扩写成专业英文 3D 提示词，再用 Tripo text-to-3D 生成。实测 DeepSeek 生成的永恒号描述非常精准。

### 11. API 设置面板 + 通用化
- 页面加「API 设置」面板，用户可填自己的 key，存在浏览器 localStorage，不上传服务器。
- 按功能命名（不绑定厂商）：3D 生成 API + AI 文字理解服务（兼容 OpenAI 格式，可填 base URL + key + 模型名，支持 DeepSeek/OpenAI/通义/Kimi 等）。

### 12. 逻辑修正：图片为主、文字为辅
用户反馈生成结果不像原图，原因是「填了出处就只走文字生成、完全丢掉图片」。
- **修正**：改为图片永远主导生成，出处文字只用来生成「AI 理解」辅助信息展示，不再抢走生成主导权。

---

## 四、已知限制

- 3D 模型精细度取决于 Tripo 免费版（默认 v2.5），升级版本 / 开纹理会额外消耗积分。
- 文生 3D 不看图，生成的是符合文字描述的模型，未必完全像原图。
- Tripo 图生 3D 接口只吃图片、不吃文字，无法「图文混合输入」（这是「图为主文为辅」只能让文字做辅助展示的技术原因）。
- 重建需 1-2 分钟（云端 AI 计算，无法加速）。
- 开发机内存偏紧，跑 3D 处理易触发内存不足导致卡顿，需释放内存或重启恢复。

---

## 五、安全提醒

- Tripo 与 LLM 的 API Key 写在 start.bat（已加入 .gitignore，不入版本库）。
- 开发过程中多个 key 曾暴露，正式使用/演示前建议到各自后台重置。
- 用户填写的 key 存在浏览器本地 localStorage，不上传服务器。

---

## 六、演示建议

- 已预生成 11 个真实模型存于 demo_models 文件夹，作为现场 fallback 保底。
- 建议提前录制完整流程演示视频，现场若卡顿可切视频。
- 演示时关闭无关程序释放内存，避免 3D 处理时卡死。
- 路演突出「情绪实体化」叙事：把电影瞬间变成能放在书桌上的实体摆件。

---

## 七、Git 版本

- 已初始化 Git 仓库并提交 v1.0（含安全处理：start.bat 等含密钥文件不入库，提供无密钥的 start.bat.template）。
