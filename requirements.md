# CineSculpt 需求文档

## 项目定位
面向 3D 打印爱好者的「电影场景实体化」 AI Agent。用户上传电影经典场景的多张截图，系统生成 3D 模型，微调后导出 STL 可直接 3D 打印成实体摆件。核心价值：把电影带来的情绪，变成可触摸的物理纪念品。

## 定位澄清（重要）
本产品定位为「生成 / 再创作」，而非「毫米级精确还原」。AI 3D 重建本质是根据图片合理「脑补」出 3D 模型，多张图用于让结果更合理。用户追求的是情绪共鸣与审美，不是精确复刻。


## 需求列表（EARS 格式）

### 需求 1：图片上传
用户故事：作为用户，我想上传同一电影场景的 4-5 张不同角度截图，以便系统据此生成 3D 模型。
验收标准：
1. 当用户选择 1-5 张图片时，系统应接受 JPG/PNG 格式并显示缩略图。
2. 当用户上传超过 5 张时，系统应提示最多 5 张。
3. 当用户未选择图片就提交时，系统应提示请先选择图片。

### 需求 2：主体提取（语义分割）
用户故事：作为用户，我希望系统只保留画面主体（如飞船、建筑），去掉背景和人物，避免打印出来糊成一团。
验收标准：
1. 当用户开启「只保留主体」选项时，系统应使用抠图模型移除背景。
2. 当抠图完成时，系统应在重建前使用处理后的图片。

### 需求 3：3D 重建（可插拔接口）
用户故事：作为用户，我想让系统根据上传图片生成 3D 网格模型。
验收标准：
1. 系统应提供统一的重建接口 reconstruct(images) 返回 3D 网格。
2. 当未配置云 API 时，系统应返回预生成的占位模型，保证流程可演示。
3. 当配置了 Meshy/Tripo3D API Key 时，系统应调用真实重建服务。
4. 当重建失败时，系统应回退到 fallback 预生成模型并提示用户。

### 需求 4：3D 预览
用户故事：作为用户，我想在网页上旋转、缩放查看生成的 3D 模型。
验收标准：
1. 当模型生成后，系统应用 Three.js 在网页展示模型。
2. 系统应支持鼠标拖拽旋转、滚轮缩放。

### 需求 5：网格微调
用户故事：作为用户，我想对模型做打印前的微调。
验收标准：
1. 加底座：系统应能在模型底部自动生成圆柱或方形底座。
2. 尺寸调整：当用户输入目标高度（如 100mm）时，系统应等比缩放模型。
3. 网格简化：系统应能减少模型面数以方便打印。
4. 一键修复：系统应检测水密性并自动补洞。

### 需求 6：打印性检测
用户故事：作为用户，我想知道当前模型能否被 3D 打印机正确打印。
验收标准：
1. 系统应估算模型在当前尺寸下的最小特征尺寸。
2. 当最小特征小于 0.5mm 时，系统应提示细节可能丢失，建议放大或切换主角模式。
3. 系统应限制单件模型最大尺寸不超过 200mm x 200mm x 200mm。

### 需求 7：场景模式 / 主角模式
用户故事：作为用户，我想自行取舍是打印完整场景（细节略糊）还是主角特写（细节清晰）。
验收标准：
1. 系统应提供「场景模式」和「主角模式」两个选项。
2. 当选择主角模式时，系统应聚焦单一主体以提升可打印细节。

### 需求 8：导出 STL
用户故事：作为用户，我想一键导出 STL 文件用于 3D 打印。
验收标准：
1. 当用户点击导出时，系统应用 Trimesh 生成 STL 文件供下载。
2. 当现场无打印机时，系统应展示「文件已生成，可发送至云打印」。

## 非目标（本次不做）
- 毫米级精确几何还原
- 多材料 / 彩色打印切片
- 真实对接物理打印机固件（用模拟流程代替）
- 剧情 / 电影名称的语义识别（对生成 3D 无必要）
