# CineSculpt 开发过程记录

> 项目：把电影截图自动转成可 3D 打印的实体摆件
> 提交人：ffxg582667-design
> 仓库：https://github.com/ffxg582667-design/CineSculpt

---

## 一、需求背景

电影带来的情绪会随时间淡掉，而市面上的周边大多是平面印刷品。本项目想解决一个问题：**如何把「电影里的一个瞬间」变成「能拿在手上的实体摆件」**。

用户的操作应该尽量简单：上传一张电影截图，剩下的链路（抠图 → 3D 重建 → 预览 → 打印前检测 → 导出）全部自动化。产品定位是**生成 / 再创作**，不是毫米级几何复刻——AI 3D 重建本质是根据图片合理「脑补」，追求情绪共鸣与审美。

---

## 二、技术方案选型

| 环节 | 选择 | 理由 |
| --- | --- | --- |
| 前端 | 原生 HTML/CSS/JS + Three.js | 无需构建工具，轻量；Three.js 直接渲染 GLB 模型，支持拖拽旋转 |
| 后端 | Python Flask + Gunicorn | 开发快、生态全；Gunicorn 是 Render 推荐的生产服务器 |
| 主体提取 | rembg (u2net) | 开源抠图模型，把主体从复杂背景中分离，合成白底提升重建质量 |
| 3D 重建 | Tripo3D SDK (image-to-model) | 真实可用的图生 3D API，返回水密 GLB |
| 场景理解 | DeepSeek | 把剧情/原著描述转成「AI 理解笔记」，辅助重建与展示 |
| 网格处理 | Trimesh + NumPy | 补洞、简化、生成底座、缩放、可打印性检测，纯本地计算 |
| 部署 | Render (Web Service) | 连 GitHub 自动部署，免费档可用，链接固定不变 |

---

## 三、系统架构

```
前端 (浏览器)
   │  HTTP / JSON
   ▼
Flask 后端 (backend/app.py)
   ├─ segment.py       rembg 抠图
   ├─ reconstruct.py   Tripo3D 图生 3D（可插拔接口 + 占位降级）
   ├─ describe.py      DeepSeek 场景理解
   └─ mesh_ops.py      网格修复 / 简化 / 底座 / 缩放 / 可打印性检测
   │
   ▼
Three.js 预览 + STL 导出
```

数据流向：上传图片 → 服务端存为 session → 抠图 → 调 Tripo3D 生成 GLB → 前端预览 → 用户微调（修复/简化/底座/缩放）→ 可打印性检测 → 导出 STL。

---

## 四、开发阶段与关键实现

1. **原型搭建**：先用 Flask 起最小服务，前后端联调上传接口，确认 session 机制可用。
2. **主体提取**：接入 rembg，处理透明背景与 alpha matting，把结果合成为白底图喂给重建模型。
3. **3D 重建**：封装 `reconstruct.py` 为可插拔接口。配置 Tripo API Key 时走真实生成；未配置时自动降级为占位模型，保证流程任何时候都能演示。
4. **网格处理**：`mesh_ops.py` 实现一键补洞（`trimesh` + `scipy`/`networkx` 连通体分析）、网格简化（`fast-simplification`）、自动生成圆柱/方形底座、按目标高度缩放，以及水密性 / 最小特征尺寸（<0.5mm 告警）/ 最大尺寸的可打印性检测。
5. **预览与导出**：Three.js 加载 GLB 在线旋转；`/api/export` 导出 STL 直接送切片软件。
6. **场景理解**：可选接入 DeepSeek，把用户给的剧情/原著文本转成「AI 理解笔记」展示在界面上。

---

## 五、AI 协作开发过程

本项目大量借助 AI 编程助手（WorkBuddy）完成工程化与上线工作，典型协作包括：

- **本地服务与公网预览**：助手帮我在本机拉起 Flask 服务，并通过 cloudflared 临时隧道生成公网地址，便于手机/他人即时预览。
- **依赖盘点与部署改造**：助手扫描全部源码的 import，补全了 `requirements.txt`、`Procfile`、`runtime.txt`，并把服务端口改为读取环境变量 `PORT`（Render 必须），避免写死 5001 导致启动失败。
- **依赖 Bug 排查**：最初 `requirements.txt` 误写 `tripo3d>=1.0`，而 PyPI 实际最新版为 0.4.2，导致 `pip install` 解析阶段立刻失败、部署 11 秒退出。助手逐一核对 PyPI 真实版本号后修正为 `tripo3d>=0.4.0`，部署恢复正常。
- **密钥安全**：`start.bat` 中含明文 API Key，助手确认它从未进入 git 历史，并加入 `.gitignore`；线上改为在 Render 环境变量中配置，避免密钥入库。
- **文档生成**：README 与本文档均由 AI 辅助整理，保证评委能快速理解项目。

---

## 六、遇到的问题与解决

| 问题 | 现象 | 解决 |
| --- | --- | --- |
| 依赖版本约束错误 | Render 构建 11.6 秒即失败 | 核对 PyPI，将 `tripo3d>=1.0` 改为 `>=0.4.0` |
| Render Root Directory 配置错 | 报 `Root directory 'src' does not exist` | 清空 Root Directory，从仓库根目录构建 |
| 服务端口写死 | 云端启动失败 | 改为读取 `$PORT` 环境变量 |
| API Key 泄露风险 | `start.bat` 明文密钥 | 加入 `.gitignore`，云端改用环境变量 |

---

## 七、部署与上线

1. 代码推送到 GitHub 仓库（公开）。
2. Render 新建 Web Service 连接该仓库，Build Command `pip install -r requirements.txt`，Start Command 为 Gunicorn 启动后端。
3. 环境变量配置 `TRIPO_API_KEY`、`DEEPSEEK_API_KEY`。
4. 部署完成后获得固定公网地址 `https://cinesculpt.onrender.com`，永久可用、全世界可访问，本机开关机不影响。

> 说明：免费档 15 分钟无访问会休眠，首次打开需等待约 30–50 秒冷启动；若部署报内存不足，删除 `rembg` 一行重新部署即可（抠图自动降级，其余功能不受影响）。

---

## 八、小结

CineSculpt 用「轻前端 + Flask 后端 + 成熟 AI 模型」的组合，把一条复杂的电影截图到 3D 打印链路压缩成几次点击。工程上最大的收获是**部署即生产**的意识：本地能跑不等于线上能跑，依赖清单、端口、密钥管理每一处都需要在真实云环境里验证。
